import Image from "next/image";

export default function Home() {
  return (
    <div className="flex items-center justify-center min-h-screen">
      <button className="px-8 py-4 text-xl font-bold bg-blue-600 text-white rounded hover:bg-blue-700 transition">
        スタート
      </button>
    </div>
  );
}
